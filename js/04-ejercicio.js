const base = prompt("Introduce la base:");
const altura = prompt("Introduce la altura:");

const b = parseInt(base);
const a = parseInt(altura);

alert("El área del triángulo con base " + a + " y altura " + b + " es: " + (a * b) / 2);
